apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "bmo.fullname" . }}-test-connection"
  labels:
    {{- include "bmo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "bmo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
