apiVersion: v1
kind: Service
metadata:
  name: {{ include "bmo.fullname" . }}
  labels:
    {{- include "bmo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "bmo.selectorLabels" . | nindent 4 }}
